apiVersion: v1
kind: Service
metadata:
  name: result-app
  labels:
    app: result-app
spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 8081
      nodePort: 32001
      name: 'result-app-service'
  selector:
    app: result-app