apiVersion: v1
kind: Service
metadata:
  name: voting-app
  labels:
    app: voting-app
spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 8080
      nodePort: 32000
      name: 'voting-app-service'
  selector:
    app: voting-app