apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: ClusterIP
  ports:
    - targetPort: 5432
      port: 5432
      name: 'postgres-service'
  selector:
    app: postgres